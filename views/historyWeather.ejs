<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/weather.css">
    <title>Weather History</title>

</head>
<body>
    <h2>Weather History</h2>
    <% if (historyWeather.length > 0) { %>
        <ul>
            <% historyWeather.forEach(entry => { %>
                <li><%= entry.city %> is <%= entry.temperature %>°C <%= entry.description %> with wind speed <%= entry.windSpeed %> m/s at <%= new Date(entry.createdAt).toLocaleString() %></li>
            <% }) %>
        </ul>
    <% } else { %>
        <p>No weather history available.</p>
    <% } %>
</body>
</html>
